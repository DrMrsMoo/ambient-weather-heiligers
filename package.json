{
  "name": "ambient-weather-heiligers",
  "version": "1.0.0",
  "description": "collect data from Heiligers weather station",
  "main": "main.js",
  "scripts": {
    "test": "./node_modules/.bin/jest",
    "start": ". ./.env && node bin/runFetchRawData.js && node bin/runConvertImperialToJsonl.js && node bin/runConvertImperialToMetric.js",
    "run_main": ". ./.env && node runMainIIFE.js",
    "backfill": ". ./.env && node bin/runBackfill.js",
    "verify-backfill": ". ./.env && node scripts/verify-backfill.js",
    "check-staging-gaps": ". ./.env && node scripts/check-recent-gaps.js",
    "check-prod-gaps": ". ./.env && node scripts/check-production-gaps.js",
    "compare-clusters": ". ./.env && node scripts/compare-clusters.js",
    "copy-prod-to-staging": ". ./.env && node scripts/copy-prod-to-staging.js",
    "analyze-data": ". ./.env && node scripts/analyze-data.js",
    "check-duplicates": ". ./.env && node scripts/check-duplicates.js",
    "check-gap-details": ". ./.env && node scripts/check-gap-details.js",
    "verify-indexing": ". ./.env && node scripts/verify-indexing.js",
    "manual-index": ". ./.env && node scripts/manual-index.js",
    "archive-data": ". ./.env && node scripts/archive-data.js",
    "archive-data:dry-run": ". ./.env && node scripts/archive-data.js --dry-run",
    "archive-logs": ". ./.env && ./scripts/archive-logs.sh",
    "archive-logs:dry-run": ". ./.env && ./scripts/archive-logs.sh --dry-run"
  },
  "author": "Tina Heiligers",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@elastic/elasticsearch": "^8.19.0",
    "ambient-weather-api": "0.0.6",
    "assert": "^2.0.0",
    "convert-units": "^2.3.4",
    "file-system": "^2.2.2",
    "moment": "^2.26.0",
    "moment-timezone": "^0.5.31",
    "readline-sync": "^1.4.10",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@elastic/elasticsearch-mock": "^2.1.0",
    "jest": "^29.7.0",
    "jest-cli": "^29.7.0",
    "jsdoc": "^3.6.7"
  }
}
